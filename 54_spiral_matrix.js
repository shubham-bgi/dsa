/**
 * @param {number[][]} matrix
 * @return {number[]}
 */
var spiralOrder = function(matrix) {
    let rows = matrix.length;
    let cols = matrix[0].length;
    let round = 0;
    //rows = 3 cols = 4
    // 1,1 - 1,2 - 1,3 - 1,4 - 2,4 - 3,4 - 3,3 - 3,2 - 3,1 - 2,1 - 2,2 - 2,3
    for(let i )
};